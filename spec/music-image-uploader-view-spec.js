'use babel';

import MusicImageUploaderView from '../lib/music-image-uploader-view';

describe('MusicImageUploaderView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
